name: Deploy Apps Script

on:
  push:
    branches: [main]
    paths: ['apps-script.js']

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Get access token
        id: token
        run: |
          RESP=$(curl -s -X POST https://oauth2.googleapis.com/token \
            -d "client_id=${{ secrets.GOOGLE_CLIENT_ID }}" \
            -d "client_secret=${{ secrets.GOOGLE_CLIENT_SECRET }}" \
            -d "refresh_token=${{ secrets.GOOGLE_REFRESH_TOKEN }}" \
            -d "grant_type=refresh_token")
          TOKEN=$(echo "$RESP" | python3 -c "import sys,json; print(json.load(sys.stdin).get('access_token',''))")
          if [ -z "$TOKEN" ]; then echo "❌ Token falhou: $RESP"; exit 1; fi
          echo "access_token=$TOKEN" >> $GITHUB_OUTPUT

      - name: Diagnose script access
        run: |
          RESP=$(curl -s \
            "https://script.googleapis.com/v1/projects/${{ secrets.CLASP_SCRIPT_ID }}" \
            -H "Authorization: Bearer ${{ steps.token.outputs.access_token }}")
          echo "=== GET SCRIPT ===" >> /tmp/diag.txt
          echo "$RESP" >> /tmp/diag.txt
          echo "$RESP"

      - name: Build payload file
        run: |
          python3 - << 'PYEOF'
          import json
          script  = open('apps-script.js').read()
          manifest = open('appsscript.json').read()
          payload = {"files":[
            {"name":"Code","type":"SERVER_JS","source":script},
            {"name":"appsscript","type":"JSON","source":manifest}
          ]}
          json.dump(payload, open('/tmp/payload.json','w'))
          print(f"✅ Payload: {len(script)} chars")
          PYEOF

      - name: Push content
        run: |
          RESP=$(curl -s -X PUT \
            "https://script.googleapis.com/v1/projects/${{ secrets.CLASP_SCRIPT_ID }}/content" \
            -H "Authorization: Bearer ${{ steps.token.outputs.access_token }}" \
            -H "Content-Type: application/json" \
            --data-binary @/tmp/payload.json)
          echo "=== PUT CONTENT ===" >> /tmp/diag.txt
          echo "$RESP" >> /tmp/diag.txt
          echo "$RESP"
          if echo "$RESP" | python3 -c "import sys,json; d=json.load(sys.stdin); exit(1 if 'error' in d else 0)"; then
            echo "✅ Código enviado"
          else
            echo "❌ Falhou — ver diag.txt"; exit 1
          fi

      - name: Save diagnostic on failure
        if: failure()
        run: |
          git config user.email "actions@github.com"
          git config user.name "GitHub Actions"
          cp /tmp/diag.txt diag.txt
          git add diag.txt
          git commit -m "debug: diagnóstico de erro [skip ci]"
          git push

      - name: Create version
        id: version
        run: |
          RESP=$(curl -s -X POST \
            "https://script.googleapis.com/v1/projects/${{ secrets.CLASP_SCRIPT_ID }}/versions" \
            -H "Authorization: Bearer ${{ steps.token.outputs.access_token }}" \
            -H "Content-Type: application/json" \
            -d "{\"description\":\"Auto deploy $(date +'%Y-%m-%d %H:%M')\"}")
          echo "$RESP"
          V=$(echo "$RESP" | python3 -c "import sys,json; print(json.load(sys.stdin).get('versionNumber',''))")
          if [ -z "$V" ]; then echo "❌ Versão falhou"; exit 1; fi
          echo "version=$V" >> $GITHUB_OUTPUT
          echo "✅ Versão $V"

      - name: Update deployment
        run: |
          RESP=$(curl -s -X PUT \
            "https://script.googleapis.com/v1/projects/${{ secrets.CLASP_SCRIPT_ID }}/deployments/${{ secrets.CLASP_DEPLOYMENT_ID }}" \
            -H "Authorization: Bearer ${{ steps.token.outputs.access_token }}" \
            -H "Content-Type: application/json" \
            -d "{\"deploymentConfig\":{\"versionNumber\":${{ steps.version.outputs.version }},\"manifestFileName\":\"appsscript\",\"description\":\"Auto deploy\"}}")
          echo "$RESP"
          if echo "$RESP" | python3 -c "import sys,json; d=json.load(sys.stdin); exit(1 if 'error' in d else 0)"; then
            echo "✅ Deploy v${{ steps.version.outputs.version }} — SUCESSO TOTAL 🎉"
          else
            echo "❌ Deploy falhou"; exit 1
          fi
